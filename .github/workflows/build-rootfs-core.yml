name: Droidspaces RootFS 核心构建

on:
  workflow_call:
    inputs:
      ui_language:
        required: false
        type: string
        default: zh
      build_target:
        required: true
        type: string
      custom_username:
        required: false
        type: string
        default: Gold
      desktop:
        required: true
        type: string
      desktop_autostart:
        required: true
        type: boolean
      display_backend:
        required: true
        type: string
      PulseAudio:
        required: true
        type: string
      enable_zh_tz:
        required: true
        type: boolean
      enable_mesa:
        required: true
        type: boolean
      enable_8gen2_wayland:
        required: true
        type: boolean
      enable_tmoe:
        required: true
        type: boolean
      nosnap:
        required: true
        type: boolean
      enable_systemd257:
        required: true
        type: boolean
      enable_srf:
        required: true
        type: boolean
      enable_binfmt:
        required: true
        type: boolean
      enable_yj:
        required: true
        type: boolean
      enable_kfgj:
        required: true
        type: boolean
      enable_zip:
        required: true
        type: boolean
      enable_docker:
        required: true
        type: boolean
      wayland_package_repository:
        required: false
        type: string
        default: Goldzxcbug/droidspaces-package

jobs:
  # --------------------------------------------------------
  # Phase 1: setup (Environment preparation and variable generation)
  # --------------------------------------------------------
  setup:
    name: ${{ inputs.ui_language == 'zh' && '准备构建' || 'Setup' }}
    runs-on: ubuntu-latest
    outputs:
      templates: ${{ steps.find-templates.outputs.templates }}
      desktop: ${{ steps.find-templates.outputs.desktop }}
      desktop_autostart: ${{ steps.find-templates.outputs.desktop_autostart }}
      display_backend: ${{ steps.find-templates.outputs.display_backend }}
      pulseaudio_effective: ${{ steps.find-templates.outputs.pulseaudio_effective }}
      build_id: ${{ steps.prep.outputs.build_id }}
      build_date: ${{ steps.prep.outputs.build_date }}
    steps:
      - name: ${{ inputs.ui_language == 'zh' && '检出仓库' || 'Checkout repository' }}
        uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}

      - name: ${{ inputs.ui_language == 'zh' && '解析构建选项' || 'Parse user build choices' }}
        id: find-templates
        env:
          BUILD_TARGET_INPUT: ${{ inputs.build_target }}
          CUSTOM_USERNAME: ${{ inputs.custom_username }}
          DESKTOP_AUTOSTART_INPUT: ${{ inputs.desktop_autostart }}
          DESKTOP_INPUT: ${{ inputs.desktop }}
          DISPLAY_BACKEND_INPUT: ${{ inputs.display_backend }}
          ENABLE_SYSTEMD257_INPUT: ${{ inputs.enable_systemd257 }}
          PULSEAUDIO_INPUT: ${{ inputs.PulseAudio }}
          UI_LANGUAGE: ${{ inputs.ui_language }}
          WAYLAND_PACKAGE_REPOSITORY: ${{ inputs.wayland_package_repository }}
        run: |
          set -euo pipefail
          source scripts/lib/desktop-config.sh

          case "$UI_LANGUAGE" in
            zh|en) ;;
            *)
              echo "不支持的界面语言 / Unsupported UI language: $UI_LANGUAGE" >&2
              exit 1
              ;;
          esac

          message() {
            if [ "$UI_LANGUAGE" = zh ]; then
              printf '%s\n' "$1"
            else
              printf '%s\n' "$2"
            fi
          }

          fail() {
            message "$1" "$2" >&2
            exit 1
          }

          TARGET="$BUILD_TARGET_INPUT"
          DESKTOP="$(desktop_normalize "$DESKTOP_INPUT")" || {
            fail "无效的桌面环境：$DESKTOP_INPUT" \
              "Invalid desktop: $DESKTOP_INPUT"
          }
          if [ "$DESKTOP" = "none" ]; then
            DISPLAY_BACKEND="x11"
          else
            DISPLAY_BACKEND="$(display_backend_normalize "$DISPLAY_BACKEND_INPUT")" || {
              fail "无效的显示后端：$DISPLAY_BACKEND_INPUT" \
                "Invalid display backend: $DISPLAY_BACKEND_INPUT"
            }
          fi
          DESKTOP_AUTOSTART="$DESKTOP_AUTOSTART_INPUT"
          PULSE_AUDIO="$PULSEAUDIO_INPUT"

          if [ "$ENABLE_SYSTEMD257_INPUT" = "true" ]; then
            case "$DESKTOP" in
              kde)
                message "已启用 systemd 257：普通 KDE 位于验证支持白名单中。" \
                  "systemd 257 is enabled: standard KDE is on the verified support allowlist."
                ;;
              none)
                DISPLAY_BACKEND="x11"
                DESKTOP_AUTOSTART="false"
                ;;
              *)
                [ "$TARGET" != "all-wayland" ] || {
                  fail "systemd 257 的 all-wayland 构建只支持普通 KDE。" \
                    "systemd 257 all-wayland builds only support standard KDE."
                }
                message "systemd 257 尚不支持 $DESKTOP：强制使用 none 桌面并关闭桌面开机自启动。" \
                  "systemd 257 does not support $DESKTOP: forcing desktop=none and disabling desktop auto-start."
                DESKTOP="none"
                DISPLAY_BACKEND="x11"
                DESKTOP_AUTOSTART="false"
                ;;
            esac
          fi

          if [[ ! "$WAYLAND_PACKAGE_REPOSITORY" =~ ^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$ ]]; then
            fail "Wayland 软件包仓库必须使用公开的“所有者/仓库名”格式。" \
              "Wayland package repository must use the public owner/repository format."
          fi
          if [[ ! "$CUSTOM_USERNAME" =~ ^[A-Za-z_][A-Za-z0-9_-]{0,31}$ ]]; then
            fail "自定义用户名必须是 1 至 32 个字符的有效 Linux 用户名。" \
              "Custom username must be a 1-32 character Linux username."
          fi

          if [ "$DESKTOP" = "kde-mobile" ] || [ "$DESKTOP" = "gnome" ] || [ "$DESKTOP" = "anland-next" ]; then
            DISPLAY_BACKEND="anland-wayland"
          fi
          if [ "$DESKTOP" = "none" ] && [ "$DESKTOP_AUTOSTART" = "true" ]; then
            fail "未安装桌面时不能启用桌面开机自启动。" \
              "Desktop auto-start cannot be enabled when desktop is none."
          fi

          case "$TARGET" in
            all)
              TEMPLATES="$({
                for dockerfile in *.Dockerfile; do
                  candidate="${dockerfile%.Dockerfile}"
                  if desktop_backend_supported "$candidate" "$DESKTOP" "$DISPLAY_BACKEND"; then
                    printf '%s\n' "$candidate"
                  fi
                done
              } | sort | jq -R -s -c 'split("\n")[:-1]')"
              ;;
            all-wayland)
              [ "$DESKTOP" != "none" ] || {
                fail "all-wayland 需要选择一个桌面环境。" \
                  "all-wayland requires a desktop profile."
              }
              DISPLAY_BACKEND="anland-wayland"
              TEMPLATES="$(desktop_wayland_targets_json "$DESKTOP")"
              ;;
            *)
              target_is_known "$TARGET" || {
                fail "未知的构建目标：$TARGET" \
                  "Unknown build target: $TARGET"
              }
              desktop_backend_supported "$TARGET" "$DESKTOP" "$DISPLAY_BACKEND" || {
                fail "不支持的发行版、桌面和显示后端组合：$TARGET/$DESKTOP/$DISPLAY_BACKEND" \
                  "Unsupported target/profile/backend: $TARGET/$DESKTOP/$DISPLAY_BACKEND"
              }
              TEMPLATES="$(jq -n -c --arg t "$TARGET" '[$t]')"
              ;;
          esac

          [ "$TEMPLATES" != "[]" ] || {
            fail "没有发行版支持 $DESKTOP/$DISPLAY_BACKEND。" \
              "No target supports $DESKTOP/$DISPLAY_BACKEND."
          }
          if [ "$DISPLAY_BACKEND" = "anland-wayland" ]; then
            PULSE_AUDIO="none"
          fi
          echo "templates=$TEMPLATES" >> "$GITHUB_OUTPUT"
          echo "desktop=$DESKTOP" >> "$GITHUB_OUTPUT"
          echo "desktop_autostart=$DESKTOP_AUTOSTART" >> "$GITHUB_OUTPUT"
          echo "display_backend=$DISPLAY_BACKEND" >> "$GITHUB_OUTPUT"
          echo "pulseaudio_effective=$PULSE_AUDIO" >> "$GITHUB_OUTPUT"
          message "最终构建矩阵：$TEMPLATES" "Final decided build matrix: $TEMPLATES"
          message "桌面环境：$DESKTOP" "Desktop profile: $DESKTOP"
          message "桌面开机自启动：$DESKTOP_AUTOSTART" \
            "Desktop auto-start: $DESKTOP_AUTOSTART"
          message "显示后端：$DISPLAY_BACKEND" "Display backend: $DISPLAY_BACKEND"
          message "Wayland 软件包仓库：$WAYLAND_PACKAGE_REPOSITORY" \
            "Wayland package repository: $WAYLAND_PACKAGE_REPOSITORY"
          message "最终 PulseAudio 设置：$PULSE_AUDIO" \
            "Final PulseAudio value: $PULSE_AUDIO"

      - name: ${{ inputs.ui_language == 'zh' && '生成构建信息' || 'Prepare build metadata' }}
        id: prep
        env:
          UI_LANGUAGE: ${{ inputs.ui_language }}
        run: |
          BUILD_DATE=$(date +'%Y-%m-%d %H:%M:%S')
          BUILD_ID="v$(date +'%Y%m%d-%H%M%S')"
          echo "build_date=$BUILD_DATE" >> "$GITHUB_OUTPUT"
          echo "build_id=$BUILD_ID" >> "$GITHUB_OUTPUT"
          if [ "$UI_LANGUAGE" = zh ]; then
            echo "构建 ID：$BUILD_ID"
          else
            echo "Build ID: $BUILD_ID"
          fi

  # --------------------------------------------------------
  # Phase 2: build (Build of regular Linux distribution RootFS)
  # --------------------------------------------------------
  build:
    name: ${{ inputs.ui_language == 'zh' && format('构建 {0}', matrix.template) || format('Build {0}', matrix.template) }}
    needs: setup
    if: ${{ needs.setup.outputs.templates != '[]' }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        template: ${{ fromJson(needs.setup.outputs.templates) }}
    steps:
      - name: ${{ inputs.ui_language == 'zh' && '检出仓库' || 'Checkout repository' }}
        uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}

      - name: ${{ inputs.ui_language == 'zh' && format('构建根文件系统（{0}）', matrix.template) || format('Build RootFS ({0})', matrix.template) }}
        env:
          ANLAND_RELEASE_REPOSITORY: ${{ inputs.wayland_package_repository }}
          CUSTOM_USERNAME: ${{ inputs.custom_username }}
        run: |
          chmod +x build_rootfs-native.sh
          ./build_rootfs-native.sh \
            -i "${{ matrix.template }}.Dockerfile" \
            -v "${{ needs.setup.outputs.build_id }}" \
            -K "${{ needs.setup.outputs.desktop }}" \
            -L "${{ needs.setup.outputs.desktop_autostart }}" \
            -B "${{ needs.setup.outputs.display_backend }}" \
            -P "${{ needs.setup.outputs.pulseaudio_effective }}" \
            -g "${{ inputs.enable_zh_tz }}" \
            -a "${{ inputs.enable_binfmt }}" \
            -b "${{ inputs.enable_yj }}" \
            -c "${{ inputs.enable_mesa }}" \
            -d "${{ inputs.enable_kfgj }}" \
            -e "${{ inputs.enable_zip }}" \
            -f "${{ inputs.enable_docker }}" \
            -h "${{ inputs.enable_srf }}" \
            -j "${{ inputs.enable_tmoe }}" \
            -n "${{ inputs.nosnap }}" \
            -S "${{ inputs.enable_systemd257 }}" \
            -t "${{ inputs.enable_8gen2_wayland }}" \
            -u "$CUSTOM_USERNAME"

      - name: ${{ inputs.ui_language == 'zh' && '上传构建产物' || 'Upload build artifacts' }}
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.template }}-${{ needs.setup.outputs.desktop }}-rootfs
          path: "*.tar.xz"
          retention-days: 1

  # --------------------------------------------------------
  # Phase 3: release (Summary and release)
  # --------------------------------------------------------
  release:
    name: ${{ inputs.ui_language == 'zh' && '发布构建结果' || 'Publish build results' }}
    needs: [setup, build]
    if: ${{ !cancelled() && needs.setup.result == 'success' && needs.build.result == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: ${{ inputs.ui_language == 'zh' && '检出仓库' || 'Checkout repository' }}
        uses: actions/checkout@v5
        with:
          ref: ${{ github.sha }}

      - name: ${{ inputs.ui_language == 'zh' && '下载全部构建产物' || 'Download all artifacts' }}
        uses: actions/download-artifact@v7
        with:
          pattern: "*-rootfs"
          merge-multiple: true

      - name: ${{ inputs.ui_language == 'zh' && '整理发布文件' || 'Organize release files' }}
        run: |
          mkdir -p release_assets
          mv *.tar.xz release_assets/

      - name: ${{ inputs.ui_language == 'zh' && '生成发布说明' || 'Prepare Release notes' }}
        env:
          UI_LANGUAGE: ${{ inputs.ui_language }}
          BUILD_TIME: ${{ needs.setup.outputs.build_date }}
          BUILD_TARGET: ${{ inputs.build_target }}
          BUILD_ID: ${{ needs.setup.outputs.build_id }}
          CUSTOM_USERNAME: ${{ inputs.custom_username }}
          DESKTOP: ${{ needs.setup.outputs.desktop }}
          DESKTOP_AUTOSTART: ${{ needs.setup.outputs.desktop_autostart }}
          DISPLAY_BACKEND: ${{ needs.setup.outputs.display_backend }}
          WAYLAND_PACKAGE_REPOSITORY: ${{ inputs.wayland_package_repository }}
          PULSE_AUDIO: ${{ needs.setup.outputs.pulseaudio_effective }}
          ENABLE_NOSNAP: ${{ inputs.nosnap }}
          ENABLE_SYSTEMD257: ${{ inputs.enable_systemd257 }}
          ENABLE_ZH_TZ: ${{ inputs.enable_zh_tz }}
          ENABLE_MESA: ${{ inputs.enable_mesa }}
          ENABLE_8GEN2_WAYLAND: ${{ inputs.enable_8gen2_wayland }}
          ENABLE_TMOE: ${{ inputs.enable_tmoe }}
          ENABLE_FCITX5: ${{ inputs.enable_srf }}
          ENABLE_BINFMT: ${{ inputs.enable_binfmt }}
          ENABLE_HARDWARE: ${{ inputs.enable_yj }}
          ENABLE_DEV_TOOLS: ${{ inputs.enable_kfgj }}
          ENABLE_COMPRESSION: ${{ inputs.enable_zip }}
          ENABLE_DOCKER: ${{ inputs.enable_docker }}
        run: |
          set -euo pipefail
          # 选项说明就是 Actions 表单上那行文本，改表单时要同步改这里
          option() { printf '%-26s: %-30s # %s\n' "$1" "$2" "$3"; }

          if [ "$UI_LANGUAGE" = zh ]; then
            {
              printf 'Droidspaces 根文件系统自动构建\n\n'
              printf '构建时间：%s\n' "$BUILD_TIME"
              printf '构建 ID：%s\n\n' "$BUILD_ID"
              printf '构建选项（按 Actions 表单顺序）：\n'
              printf '```\n'
              option build_target "$BUILD_TARGET" "选择要构建的发行版"
              option custom_username "$CUSTOM_USERNAME" "自定义用户名"
              option desktop "$DESKTOP" "桌面选择"
              option desktop_autostart "$DESKTOP_AUTOSTART" "桌面开机自启动"
              option display_backend "$DISPLAY_BACKEND" "显示后端（KDE mobile/GNOME/Anland Next 固定使用 Anland Wayland）"
              option PulseAudio "$PULSE_AUDIO" "PulseAudio 音频转发"
              option enable_zh_tz "$ENABLE_ZH_TZ" "使用中文并设置上海时区"
              option enable_mesa "$ENABLE_MESA" "开启高通骁龙 GPU 支持"
              option enable_8gen2_wayland "$ENABLE_8GEN2_WAYLAND" "修复骁龙 8 Gen 2 Wayland 花屏"
              option enable_tmoe "$ENABLE_TMOE" "集成 TMOE"
              option nosnap "$ENABLE_NOSNAP" "移除 Ubuntu Snap"
              option enable_systemd257 "$ENABLE_SYSTEMD257" "使用 systemd 257 包族（仅支持 none 和普通 KDE）"
              option enable_srf "$ENABLE_FCITX5" "开启 fcitx5 输入法"
              option enable_binfmt "$ENABLE_BINFMT" "开启跨架构支持"
              option enable_yj "$ENABLE_HARDWARE" "开启 NAT 和硬件识别"
              option enable_kfgj "$ENABLE_DEV_TOOLS" "安装开发工具"
              option enable_zip "$ENABLE_COMPRESSION" "安装压缩工具"
              option enable_docker "$ENABLE_DOCKER" "安装 Docker"
              option wayland_package_repository "$WAYLAND_PACKAGE_REPOSITORY" "Wayland 软件包仓库（公开的 owner/repository）"
              printf '```\n\n'
              printf 'Anland 音频说明：Wayland 模式使用 Anland 应用内置音频，无需 PulseAudio\n'
            } > release-notes.md
          else
            {
              printf 'Droidspaces RootFS Automated Build\n\n'
              printf 'Build Time: %s\n' "$BUILD_TIME"
              printf 'Build ID: %s\n\n' "$BUILD_ID"
              printf 'Build Options (in Actions form order):\n'
              printf '```\n'
              option build_target "$BUILD_TARGET" "Distribution to build"
              option custom_username "$CUSTOM_USERNAME" "Custom username"
              option desktop "$DESKTOP" "Desktop"
              option desktop_autostart "$DESKTOP_AUTOSTART" "Start desktop on boot"
              option display_backend "$DISPLAY_BACKEND" "Display backend (KDE mobile/GNOME/Anland Next always use Anland Wayland)"
              option PulseAudio "$PULSE_AUDIO" "PulseAudio forwarding"
              option enable_zh_tz "$ENABLE_ZH_TZ" "Use Chinese language and Asia/Shanghai timezone"
              option enable_mesa "$ENABLE_MESA" "Enable Qualcomm Snapdragon GPU support"
              option enable_8gen2_wayland "$ENABLE_8GEN2_WAYLAND" "Fix Snapdragon 8 Gen 2 Wayland corruption"
              option enable_tmoe "$ENABLE_TMOE" "Integrate TMOE"
              option nosnap "$ENABLE_NOSNAP" "Remove Ubuntu Snap"
              option enable_systemd257 "$ENABLE_SYSTEMD257" "Use the systemd 257 package family (none and standard KDE only)"
              option enable_srf "$ENABLE_FCITX5" "Enable fcitx5"
              option enable_binfmt "$ENABLE_BINFMT" "Enable cross-architecture support"
              option enable_yj "$ENABLE_HARDWARE" "Enable NAT and hardware recognition"
              option enable_kfgj "$ENABLE_DEV_TOOLS" "Install development tools"
              option enable_zip "$ENABLE_COMPRESSION" "Install compression tools"
              option enable_docker "$ENABLE_DOCKER" "Install Docker"
              option wayland_package_repository "$WAYLAND_PACKAGE_REPOSITORY" "Wayland package repository (public owner/repository)"
              printf '```\n\n'
              printf 'Anland audio note: Wayland mode uses the built-in Anland app audio, so PulseAudio is not needed\n'
            } > release-notes.md
          fi

      - name: ${{ inputs.ui_language == 'zh' && '创建草稿 Release 并上传文件' || 'Stage draft Release and upload assets' }}
        id: stage-release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.setup.outputs.build_id }}
          target_commitish: ${{ github.sha }}
          name: ${{ inputs.ui_language == 'zh' && format('Droidspaces 根文件系统 {0}', needs.setup.outputs.build_id) || format('Droidspaces RootFS {0}', needs.setup.outputs.build_id) }}
          files: "release_assets/*"
          body_path: release-notes.md

          draft: true
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: ${{ inputs.ui_language == 'zh' && '发布已验证的 Release' || 'Publish staged Release' }}
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_ID: ${{ steps.stage-release.outputs.id }}
          REPOSITORY: ${{ github.repository }}
          UI_LANGUAGE: ${{ inputs.ui_language }}
        run: |
          set -euo pipefail
          case "$RELEASE_ID" in
            ''|*[!0-9]*)
              if [ "$UI_LANGUAGE" = zh ]; then
                echo "Release 操作没有返回有效的草稿 Release ID。" >&2
              else
                echo "Release action did not return a valid draft Release ID." >&2
              fi
              exit 1
              ;;
          esac
          gh api \
            --method PATCH \
            "repos/$REPOSITORY/releases/$RELEASE_ID" \
            -F draft=false \
            --silent
